# Everything that needs no neural model. The owner has confirmed that a
# missing reference manifest and unapproved taxonomy block detector
# selection and evaluation only, so health, calibration QA, motion and
# segmentation proceed without them (doubt.md 0.4).
experiment_id: phase0_to_3_no_models
description: Ingest, health, calibration, motion, ROI and segmentation only.
seed: 20260818
artifact_root: artifacts/runs
residency: sequential
vram_safety_margin_gb: 2.0
configs:
  - seat_only_no_track
stages:
  - source
  - 01_ingest
  - 02_health
  - 03_alignment
  - 04_calibration
  - 05_motion_roi
  - 06_segmentation
  - 13_reports
